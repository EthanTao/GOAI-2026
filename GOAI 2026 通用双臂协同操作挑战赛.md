# GOAI 2026 通用双臂协同操作挑战赛
## Agent 执行与复现任务书

> **目标：让 Agent 从零完成 GOAI 2026 初赛 baseline 的环境搭建、RoboDojo/XPolicyLab/ACT 跑通、本地冒烟测试，并形成后续算法改进的基础。**

---

# 0. 项目目标

本项目参加 **GOAI 2026 · 通用双臂协同操作挑战赛**。

官方要求：

- 使用 RoboDojo / X-Eval 评测体系
- 初赛评测 Generalization 维度
- 共 12 个基础任务
- 每个任务包含 standard 与 `_random` 配置
- 共 24 个可运行配置
- 每支队伍最多提交 3 次有效成绩
- 初赛取最高成绩
- 最终需要提供：
  - 可运行代码
  - Policy Server
  - 评测结果
  - GitHub/Git 仓库

本阶段**不要尝试设计新的模型**。

第一目标只有一个：

> **完整跑通官方 baseline，并完成 24 个配置的本地冒烟测试。**

第二目标：

> **理解 RoboDojo → Observation → Policy → Action → Environment 的完整数据流。**

第三目标：

> **在 baseline 基础上进行第一轮可控修改，为后续 VLA / WAM / World Model 研究建立实验基础。**

---

# 1. 官方资源

## GOAI 官网

https://www.goaihz.com/

## GOAI 2026 赛事页面

https://xsparkai.com/goai-2026/

## 参赛申请

https://xsparkai.com/goai-2026/apply

## RoboDojo

https://github.com/RoboDojo-Benchmark/RoboDojo

## RoboDojo 文档

https://robodojo-benchmark.com/

## XPolicyLab

https://github.com/XPolicyLab/XPolicyLab

## GOAI-2026 Dataset

Hugging Face：

https://huggingface.co/datasets/RoboDojo-Benchmark/GOAI-2026

ModelScope：

https://www.modelscope.cn/datasets/RoboDojo-Benchmark/GOAI-2026

---

# 2. 重要约束

## 2.1 不要修改官方 benchmark

不要修改：

- RoboDojo task definition
- evaluation logic
- reward / scoring
- randomization
- official task configuration

如果需要修改代码：

> 优先修改 Policy / Adapter / Training Pipeline。

---

## 2.2 不要直接开始设计新模型

第一阶段必须：

```text
官方环境
    ↓
官方 baseline
    ↓
完整运行
    ↓
记录结果
    ↓
理解数据流
    ↓
再修改
```

禁止一开始就：

- 重写 ACT
- 自己设计 VLA
- 自己设计 World Model
- 修改任务环境
- 修改评分逻辑

---

# 3. 推荐环境

优先使用：

```text
Ubuntu 22.04 / 24.04
Python / Conda
NVIDIA GPU
CUDA
Git
Git LFS
```

如果当前机器是 Windows：

优先考虑：

### 方案 A

Windows + WSL2 + Ubuntu

### 方案 B

Linux 云服务器

### 方案 C

Linux 双系统

如果 RoboDojo 对 Windows 原生支持存在兼容性问题：

> 不要浪费大量时间解决 Windows 原生兼容问题，直接切换 Linux。

---

# 4. 第一阶段：检查机器环境

Agent 首先执行环境检查。

检查：

```bash
uname -a
lsb_release -a
python --version
conda --version
git --version
git lfs version
nvidia-smi
nvcc --version
```

同时检查：

```bash
df -h
free -h
```

确认：

- GPU 型号
- GPU 显存
- CUDA 版本
- 磁盘空间
- RAM
- Python
- Conda

将结果保存：

```text
docs/environment.md
```

格式：

```markdown
# Environment

## OS

## CPU

## RAM

## GPU

## VRAM

## CUDA

## Python

## Conda

## Disk

## Notes
```

---

# 5. 第二阶段：获取 RoboDojo

执行：

```bash
git lfs install

git clone --recurse-submodules \
https://github.com/RoboDojo-Benchmark/RoboDojo.git

cd RoboDojo
```

如果已经存在：

```bash
git submodule update --init --recursive
```

检查：

```bash
git status
git submodule status
```

记录当前 commit：

```bash
git rev-parse HEAD
```

保存到：

```text
docs/versions.md
```

---

# 6. 第三阶段：安装 RoboDojo

按照官方要求执行：

```bash
bash scripts/install.sh -i
```

然后：

```bash
conda activate RoboDojo
```

验证：

```bash
python --version
python -c "import torch; print(torch.__version__)"
```

如果失败：

1. 不要盲目修改依赖；
2. 记录完整错误；
3. 判断是：
   - CUDA
   - Python
   - Conda
   - PyTorch
   - 编译依赖
   - 系统依赖
4. 尽可能遵循官方版本。

---

# 7. 第四阶段：下载 GOAI 专用 Assets

### 非常重要

GOAI 2026 不使用 RoboDojo 默认 assets 下载流程。

**不要执行：**

```bash
scripts/init_assets.sh
```

使用 GOAI-2026 dataset。

安装：

```bash
python -m pip install -U huggingface_hub
```

下载：

```bash
hf download RoboDojo-Benchmark/GOAI-2026 \
  --repo-type dataset \
  --include "Assets/**" \
  --local-dir .
```

然后：

```bash
python utils/update_embodiment_config_path.py
```

如果 Hugging Face 下载困难：

使用 ModelScope：

```bash
python -m pip install -U modelscope
```

然后：

```bash
modelscope download \
  --dataset RoboDojo-Benchmark/GOAI-2026 \
  --include "Assets/**" \
  --local_dir .
```

然后：

```bash
python utils/update_embodiment_config_path.py
```

---

# 8. 第五阶段：下载 GOAI 专用数据

不要执行：

```bash
scripts/RoboDojo/download_data.sh
```

直接：

```bash
hf download RoboDojo-Benchmark/GOAI-2026 \
  --repo-type dataset \
  --include "data/hdf5/**" \
  --local-dir .
```

或者：

```bash
modelscope download \
  --dataset RoboDojo-Benchmark/GOAI-2026 \
  --include "data/hdf5/**" \
  --local_dir .
```

检查：

```bash
find data -maxdepth 3 -type f | head
```

---

# 9. 第六阶段：确认 12 个任务

需要确认以下任务存在：

```text
stack_bowls
push_T
pack_objects_into_box
fold_clothes
hang_mugs
sweep_blocks
pour_liquid_into_cup
make_toast
arrange_largest_number
sort_nesting_dolls_by_size
store_laptop_and_headphones
stack_blocks
```

每个任务应存在：

```text
standard
random
```

总计：

```text
12 × 2 = 24 configurations
```

---

# 10. 第七阶段：安装 XPolicyLab

获取：

```bash
git clone https://github.com/XPolicyLab/XPolicyLab.git
```

检查其 README 和 ACT 文档。

重点理解：

```text
policy/
ACT/
```

以及：

```text
policy adapter
training
inference
server
```

不要立即修改代码。

首先回答：

1. XPolicyLab 如何定义 Policy？
2. Observation 从哪里来？
3. Action 从哪里产生？
4. ACT 接收什么输入？
5. ACT 输出什么？
6. RoboDojo 如何调用 Policy？
7. Policy Server 如何工作？

结果保存：

```text
docs/xpolicylab_dataflow.md
```

---

# 11. 第八阶段：跑通 ACT baseline

官方推荐参考：

XPolicyLab ACT README：

https://github.com/XPolicyLab/XPolicyLab/blob/main/policy/ACT/README.md

目标：

> **完整跑通 ACT baseline。**

不要只完成：

```text
import 成功
```

必须完成：

```text
Dataset
    ↓
Training / Checkpoint
    ↓
Policy
    ↓
RoboDojo
    ↓
Action
    ↓
Environment
```

如果官方已经提供 checkpoint：

优先使用官方 checkpoint。

如果必须训练：

使用官方提供的数据和训练配置。

---

# 12. 第九阶段：理解 Action Type

GOAI 允许：

```text
ACTION_TYPE=ee
```

或者：

```text
ACTION_TYPE=joint
```

必须确认 ACT 当前使用哪一种。

最终：

> 本地测试、Policy Server、正式提交必须保持一致。

不要在不同阶段随意切换。

---

# 13. 第十阶段：完成 Smoke Test

设置：

```bash
POLICY_DIR="XPolicyLab/policy/YOUR_POLICY"
POLICY_ENV="YOUR_POLICY_CONDA_ENV"
RUN_LABEL="YOUR_RUN_LABEL"
ACTION_TYPE=ee
```

实际值由 Agent 根据环境填写。

运行：

```bash
bash scripts/robodojo.sh smoke \
  --dimension generalization \
  --policy-dir "${POLICY_DIR}" \
  --ckpt "${RUN_LABEL}" \
  --policy-env "${POLICY_ENV}" \
  --eval-env RoboDojo \
  --action-type "${ACTION_TYPE}" \
  --eval-num 1
```

必须使用：

```text
--dimension generalization
```

不能遗漏。

---

# 14. Smoke Test 的目标

Smoke Test 不是为了取得高分。

它只负责确认：

```text
RoboDojo
    ↓
24 configurations
    ↓
Policy
    ↓
Action
    ↓
Environment
```

整条链可以运行。

每个 configuration：

```text
1 episode
```

共：

```text
24 episodes
```

---

# 15. 建立测试结果表

建立：

```text
results/smoke_test.csv
```

字段：

```text
task
variant
success
episode
error
runtime
notes
```

例如：

```csv
task,variant,success,episode,error,runtime,notes
stack_bowls,standard,true,1,,...
stack_bowls,random,false,1,,...
push_T,standard,true,1,,...
```

不要只保存“成功/失败”。

如果失败：

必须记录：

- error message
- 日志
- screenshot（如果方便）
- failure behavior
- 是否为环境问题
- 是否为 policy 问题

---

# 16. 建立日志系统

目录：

```text
logs/
```

推荐：

```text
logs/
├── install/
├── smoke/
├── training/
├── inference/
└── server/
```

每一次重要运行都应该留下日志。

不要出现：

> “刚才好像跑成功了，但是不知道为什么。”

---

# 17. 建立 Checkpoint 管理

目录：

```text
checkpoints/
```

至少记录：

```text
model name
training config
dataset
commit
date
action type
performance
```

建立：

```text
docs/checkpoints.md
```

例如：

```markdown
# Checkpoints

## ACT baseline

- Model:
- Commit:
- Dataset:
- Action type:
- Training config:
- Checkpoint:
- Smoke test:
- Notes:
```

---

# 18. 第十一阶段：建立正式 Evaluation 流程

在 Smoke Test 完全通过之前：

> **禁止进行正式提交。**

正式评测最多：

```text
3 次有效成绩
```

所以：

```text
本地 smoke test
        ↓
本地完整测试
        ↓
检查 Policy Server
        ↓
确认模型固定
        ↓
确认 action type
        ↓
确认配置
        ↓
正式提交
```

---

# 19. Policy Server

官方要求：

> Policy Server 必须提供公网 WebSocket endpoint。

形式：

```text
wss://host:port
```

申请表填写：

```text
host
port
```

不能使用：

```text
localhost
127.0.0.1
192.168.x.x
10.x.x.x
172.16.x.x
```

等内网地址。

---

# 20. Policy Server 部署要求

最终服务器至少需要：

```text
公网 IP / 域名
      ↓
开放端口
      ↓
Firewall
      ↓
TLS
      ↓
WebSocket
      ↓
Policy Server
      ↓
Model
```

如果使用云服务器：

需要确认：

- Security Group
- Ubuntu Firewall
- Port forwarding
- TLS
- WebSocket
- DNS（如果使用域名）

---

# 21. 正式申请信息

需要准备：

```text
队伍名称
联系人
手机号
邮箱
Policy Server Host
Policy Server Port
策略名称
Action Type
```

策略名称：

> 只能使用字母、数字、下划线。

例如：

```text
ACT_baseline_v1
```

---

# 22. 正式评测期间的纪律

提交正式评测后：

> **不要更换模型。**

> **不要更换 action type。**

> **不要修改协议行为。**

> **不要修改 Policy Server。**

保证：

```text
本地测试版本
=
提交版本
=
服务器版本
```

---

# 23. GOAI 官方完整流程

最终流程：

```text
① GOAI 官网注册
        ↓
② 官网报名
        ↓
③ 本地 RoboDojo 环境
        ↓
④ XPolicyLab
        ↓
⑤ ACT baseline
        ↓
⑥ 24 configuration smoke test
        ↓
⑦ Policy Server
        ↓
⑧ 申请评测
        ↓
⑨ 官方评测
        ↓
⑩ 收到评测结果邮件
        ↓
⑪ 保存邮件
        ↓
⑫ GOAI 官网提交作品
```

---

# 24. 第一阶段完成标准

Agent 只有完成以下内容，才算第一阶段完成。

## Environment

- [ ] Linux 环境正常
- [ ] NVIDIA GPU 可用
- [ ] CUDA 可用
- [ ] Conda 可用
- [ ] Git LFS 可用

## RoboDojo

- [ ] RoboDojo clone
- [ ] submodules 正常
- [ ] RoboDojo 安装成功
- [ ] GOAI Assets 下载成功
- [ ] GOAI data 下载成功

## XPolicyLab

- [ ] XPolicyLab 安装成功
- [ ] ACT dependency 正常
- [ ] ACT baseline 可运行

## Evaluation

- [ ] `stack_bowls`
- [ ] `push_T`
- [ ] `pack_objects_into_box`
- [ ] `fold_clothes`
- [ ] `hang_mugs`
- [ ] `sweep_blocks`
- [ ] `pour_liquid_into_cup`
- [ ] `make_toast`
- [ ] `arrange_largest_number`
- [ ] `sort_nesting_dolls_by_size`
- [ ] `store_laptop_and_headphones`
- [ ] `stack_blocks`

以及：

- [ ] 12 standard
- [ ] 12 random
- [ ] 24 configuration smoke test

---

# 25. Agent 工作原则

## 原则 1：先跑通，再理解，再优化

不要：

```text
看代码
↓
大规模重构
↓
最后发现环境没跑通
```

应该：

```text
跑通
↓
记录
↓
理解
↓
最小修改
↓
验证
```

---

## 原则 2：遇到错误不要直接重装

错误发生时：

1. 保存完整日志；
2. 判断错误来源；
3. 搜索官方 repo / issue；
4. 检查版本；
5. 最小修改；
6. 再测试。

---

## 原则 3：每次修改必须可回滚

使用 Git。

每完成一个稳定阶段：

```bash
git status
git add ...
git commit
```

推荐 commit：

```text
env: setup RoboDojo
env: install XPolicyLab
feat: run ACT baseline
test: pass smoke test
infra: deploy policy server
```

---

# 26. 第二阶段：开始理解 ACT

第一阶段跑通之后，开始研究：

```text
ACT
```

重点理解：

```text
Image
State
Action
Action Chunking
Transformer
CVAE
Temporal prediction
```

需要回答：

### Q1

ACT 输入是什么？

### Q2

ACT 输出是什么？

### Q3

为什么不是只预测一个 action？

### Q4

Action Chunking 是什么？

### Q5

ACT 为什么适合 imitation learning？

### Q6

ACT 对 unseen/random environment 的泛化能力来自哪里？

---

# 27. 第三阶段：建立自己的 Baseline

完成官方 ACT 后：

建立 baseline：

```text
ACT-v0
```

记录：

```text
task success
runtime
failure mode
generalization
```

然后才开始：

```text
ACT-v1
```

每次只改一个因素。

例如：

```text
ACT-v0
 ↓
better visual encoder
 ↓
ACT-v1
```

或者：

```text
ACT-v0
 ↓
data augmentation
 ↓
ACT-v2
```

或者：

```text
ACT-v0
 ↓
action representation
 ↓
ACT-v3
```

禁止一次修改 5 个东西。

---

# 28. 后续研究方向

第一阶段稳定之后，再考虑：

## 路线 A：VLA

```text
Vision
+
Language
+
Action
```

研究：

- OpenVLA
- SmolVLA
- Pi0
- Pi0.5
- X-VLA
- 其他 VLA baseline

目标：

> 提升语言理解与任务泛化能力。

---

## 路线 B：World Model / WAM

研究：

```text
Observation
+
Action
↓
Future prediction
```

再进一步：

```text
Current State
      ↓
Candidate Actions
      ↓
World Model
      ↓
Future States
      ↓
Planning
      ↓
Action
```

目标：

> 提升长时序任务和复杂操作的规划能力。

---

# 29. 当前阶段禁止做的事情

在 ACT baseline 完全跑通之前：

- [ ] 不设计新的 World Model
- [ ] 不重写 RoboDojo
- [ ] 不修改评分
- [ ] 不修改任务
- [ ] 不追求 SOTA
- [ ] 不同时研究多个 Policy
- [ ] 不同时修改多个变量
- [ ] 不进行正式比赛提交

---

# 30. 最终目标

最终形成：

```text
GOAI_Project/
│
├── RoboDojo/
│
├── XPolicyLab/
│
├── policy/
│   └── custom_policy/
│
├── checkpoints/
│
├── configs/
│
├── logs/
│
├── results/
│   ├── smoke_test.csv
│   ├── baseline.csv
│   └── experiments.csv
│
├── docs/
│   ├── environment.md
│   ├── versions.md
│   ├── xpolicylab_dataflow.md
│   ├── checkpoints.md
│   ├── experiments.md
│   └── troubleshooting.md
│
└── README.md
```

---

# 31. Agent 第一阶段立即执行任务

现在开始，不要等待进一步确认。

按照以下顺序：

```text
1. 检查系统环境
2. 检查 GPU/CUDA
3. 获取 RoboDojo
4. 初始化 submodules
5. 安装 RoboDojo
6. 下载 GOAI Assets
7. 下载 GOAI data
8. 验证 12 个任务
9. 获取 XPolicyLab
10. 安装 ACT
11. 阅读 ACT README
12. 找到官方 baseline / checkpoint
13. 跑通单任务
14. 跑通 standard
15. 跑通 random
16. 执行 24 configuration smoke test
17. 保存全部日志
18. 生成 smoke_test.csv
19. 输出当前成功率
20. 总结剩余问题
```

---

# 32. Agent 汇报格式

每完成一个阶段，使用以下格式汇报：

```markdown
# Stage Report

## Stage

例如：
RoboDojo Installation

## Status

PASS / PARTIAL / FAILED

## Environment

- OS:
- Python:
- CUDA:
- GPU:

## Actions

执行了什么。

## Result

结果是什么。

## Files Changed

修改了哪些文件。

## Logs

日志在哪里。

## Problems

遇到了什么问题。

## Next Step

下一步做什么。

## Reproduction Command

给出能够重新运行的命令。
```

---

# 33. 最重要的最终原则

这个项目不是：

> “帮我写一个机器人模型。”

而是：

> **帮我建立一个可以持续实验、持续 benchmark、持续迭代的 GOAI 2026 研究与比赛平台。**

最终目标：

```text
               GOAI
                 │
          ┌──────┴──────┐
          │             │
      Benchmark       Platform
          │             │
       RoboDojo      XPolicyLab
          │             │
          └──────┬──────┘
                 │
              Baseline
                 │
                ACT
                 │
          ┌──────┴──────┐
          │             │
         VLA           WAM
          │             │
          └──────┬──────┘
                 │
          Custom Policy
                 │
                 ▼
          GOAI 2026 Final
```

**第一目标：跑通。**

**第二目标：建立 baseline。**

**第三目标：理解。**

**第四目标：改进。**

**第五目标：形成自己的 VLA / WAM / 双臂操作方案。**